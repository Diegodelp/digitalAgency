import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "DigitalPro Agency - Desarrollo de Productos Digitales",
  description: "Agencia especializada en desarrollo de aplicaciones web, APIs y productos digitales personalizados",
  keywords: "desarrollo web, aplicaciones, APIs, productos digitales, agencia desarrollo",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="es">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
